-- ============================================================
-- ACCOUNT PARAMETERS
-- RUN ONCE PER ACCOUNT.
-- Account-level parameter defaults (e.g. local timezone).
-- ============================================================
USE ROLE ACCOUNTADMIN;

ALTER ACCOUNT SET TIMEZONE = 'America/Toronto';
